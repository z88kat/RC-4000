// jquery is ready
$(document).ready(function () {
    // init the application
    initApplication();
});

const initApplication = () => {
    // set the timestamp
    $('#timestamp').html('2021-01-01 00:00:00');
}